import { Typography } from "@mui/material";

const resetPassword = () => {
  return <Box>
    <Typography variant="">
        Reset Password
    </Typography>
  </Box>;
};

export default resetPassword;
